node_modules/
dist/

*.bun-build
*.tsbuildinfo
*.log

.env

.vscode/
.zed/
.idea/
*.swp
*.swo
*~

.npm/

.pi_config/
tui-debug.log
compaction-results/
syntax.jsonl
out.jsonl
pi-*.html
out.html
todo.md

resources/
/.omx
/.pi/loom/state.json
# Loom keeps canonical project state under .loom/ and that state belongs in git.
# Ignore only clone-local execution scaffolding and local durable control-plane state that does not define shared repo truth.
# Worker records persist locally under .loom/workers/ but tickets remain the shared execution ledger.
.loom/workers/
.loom/**/launch.json
.loom/runtime/

/catalog.*
/.ralph-worktrees
/state/scope-bindings.json
